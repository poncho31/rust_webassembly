[workspace]
resolver = "2"
members = [
    "server",
    "core",
    "client"
]

[workspace.dependencies]
sqlx = { version = "0.7.1", features = [ "runtime-tokio", "tls-native-tls", "postgres", "uuid", "time", "macros" ] }
tokio = { version = "1.32", features = ["full"] }
